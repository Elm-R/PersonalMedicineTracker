# Start from official MySQL 8 image
FROM mysql:8.0

# Copy backup.sql from project root (two levels up from Dockerfile)
COPY backup.sql /docker-entrypoint-initdb.d/

# (Optional: ensure native auth + local infile allowed)
CMD ["--default-authentication-plugin=mysql_native_password", "--local-infile=1"]
